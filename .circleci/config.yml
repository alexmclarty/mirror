version: 2
jobs:
  build:
    docker:
      - image: python:3
    steps:
      - checkout
      - run:
          command: |  # use pipenv to install dependencies
            pip install pipenv
            pipenv install
      - run:
          pytest tests
